# Windows 原生桌面版实施计划

日期：2026-09-14

当前视觉优先级（2026-09-16 用户反馈）：收紧整套 UI 的 padding/间距，并明确使用图标。
- [x] 全局按钮/输入/页签/列表行及录音条、库、设置的紧凑调整；主要操作增加主题感知矢量图标。
- [x] 本轮完整 WPF 回归、x64/ARM64 发布；x64 录音库明暗/最小窗口与设置的限定实窗视频复核，见验证记录。
- [x] 修复最小库本机详情嵌套滚动容器吞掉滚轮；WPF 往返回归、独立库滚轮回归及实际最小窗口视频两项通过，见 `local-wheel-ui-01`。
- [ ] 补齐其余页面全部状态、多 DPI 与键盘焦点的视觉检查；云端页签/触控板等滚动组合仍需逐项验收。
- [x] 本机/云端播放器改为按键重复时及时定位、焦点停留时继续刷新进度；真实媒体回归与生产控件独立窗口 Tab/Home 视频两项通过。物理长按、完整 App 云播放及其他 DPI 未验，见 [验证记录](windows-native-validation.md)。
- 2026-09-16 键盘检查修复本机/云端进度条收到 Tab、Shift、空格等无关按键时误跳转；旧代码失败复现、完整 WPF 回归及双架构发布通过。真实键盘视频、焦点刷新及长按仍待验，见 [验证记录](windows-native-validation.md)。

## 目标与约束

- [x] 设备登录响应返回后重验验证码期限，拒绝迟到授权；补齐 pending/slow_down/拒绝/过期/取消及非法响应回归。核心 35 组、完整 WPF 和双架构发布通过；真实浏览器登录保持待验，见 [验证记录](windows-native-validation.md)。

- [x] 丢弃确认与字幕清理期间绑定原录音会话，迟到调用不删除新录音或已保存音频；原生确认默认保留。核心 34 组、完整 WPF、双架构发布及完整 App 确认/保留继续/丢弃视频三项通过，其他录音哈希不变。真实字幕连接竞争、全部键盘与多 DPI 未验，见 [验证记录](windows-native-validation.md)。

- [x] 完整 x64 App 真实合成样本的摘要生成、问答、TXT 导出、重转写保留两版与新版本读取；视频五项通过，测试云样本/临时凭据清理完成。完整弹窗布局和真实草稿时序两项证据不足，见 [云端回顾验证](windows-native-cloud-ui-validation.md)；不补勾整个回顾阶段。

- [x] 问答发送时清空、完成保留新草稿、失败/取消恢复未编辑原问题；9 种状态回归与受控原生窗口视频两项通过，见 [草稿验证](windows-native-ask-draft-validation.md)。真实云端草稿流程仍待验。
- [x] 问答历史与推荐独立加载，推荐慢响应不阻塞发送；旧推荐/回答跨录音隔离回归及原生受控窗口视频通过。真实云端慢响应仍待验，证据同上。

交付可运行、可安装的 Windows 原生 Recappi Mini，与 macOS 已实现功能对齐。整体流程、信息层级、窗口职责、主要布局与交互习惯以 macOS 版为基准；控件视觉可采用 Windows 原生样式，不追求像素复刻。优先低空闲开销、可靠录音和可维护性。

- 使用 C# 原生桌面技术，不使用 Tauri、Electron、WebView 或浏览器 UI。
- 已完成 WinUI 3/WPF 基础探针比较，采用 WPF + C#；依据和测试局限见 [验证记录](windows-native-validation.md)。不得把探针体积当作完整应用开销。
- 复用现有 Windows WASAPI 系统音频、进程音频和麦克风采集代码；保持现有 CLI、macOS 和协议兼容。
- 桌面版运行不依赖用户安装 Node。原有 TS API/录音编排代码作为协议参考，需要共享能力时优先抽取 C# 核心而非再包一层 CLI。
- macOS 的系统专有交互按 Windows 等价行为实现；源码中的未实现占位不冒充已支持功能。
- 本计划和所有验收证据持续更新。截图、演示数据、单元测试和真实后端验证分别标注，不互相替代。
- 2026-09-15 用户进一步明确：外观可以不同，但整体流程、layout、UX 应尽量一致。需逐屏核对原版导航、内容分区、主次操作与状态转换；当前 Windows 的独立本地库/云库入口、长表单设置等不能仅以功能存在视为对齐完成。
- 2026-09-16 用户明确改为直接合入默认分支；已通过合并提交 `8ac09de` 将原功能分支纳入 `main`。后续按此授权推进，未完成的验收项继续保留，不把合并视为产品验收完成。
- 2026-09-15 用户要求 UI 验收录制视频、提取去重关键帧、分析关键 UI 并自动验收。后续 UI 验收必须保留视频、帧时间点和通过/失败/证据不足判定；时间线未覆盖、画面遮挡或采样不足不得判通过。

## 阶段 1：功能基线与原生技术验证

- 2026-09-16 说话人流程补验：完整 App 真实转写后编辑弹窗完整显示、未修改取消返回视频两项通过；实际姓名输入受工具限制，保存/重启保持仍证据不足。新增修改后关闭、账号/录音切换、清空及校验恢复五类真实 WPF 弹窗回归，完整套件通过，见 [验证记录](windows-native-validation.md)。不补勾整个回顾阶段。

- 2026-09-16 大文件实操补验：完整 x64 App 将 344,649,225 B 合成 MP3 转码为 2,757,189,214 B WAV，自动入库并显示 07:58:40；近末尾 seek 后播放/暂停视频两项通过。原始视频、10 张去重关键帧、判定与磁盘验证位于 `build/native-desktop-validation/import-complete-ui-01`。输出 48 kHz/16-bit/单声道、28720.7205 秒，源哈希不变、末五秒 PCM 可读且非零。产物 `1089045cc2fd49cd9851993f1cadf0ec` 是通知修复提交前构建（报告 `10f0bd6`、dirty），未新改生产代码。147.5 秒无声视频仅捕获库窗口，不验文件对话框或八小时连续播放；其他格式/云详情期间导入仍待验。

- 2026-09-16 性能补测：完整 x64 App 同一进程的录音条、空录音库打开、关闭后三段约 25.6 秒空闲 CPU 为单核的 0% / 0.061% / 0%。关闭后私有内存未回落，不据此判断泄漏；产物源码标记、UI 检查影响与原始数据见 [性能报告](windows-native-performance.md)。长期、冷启动和 ARM64 实机仍未验，不补勾完整性能基线。

- [x] 2026-09-16 关闭生命周期问题已定位并修复：日期分组回调捕获窗口，令 WPF 缓存视图保持整个录音库。改为独立时钟委托后，真实八轮播放/搜索/关闭的 24 个对象及音频句柄均释放，新增默认缓存引用回归且完整 WPF 回归通过。诊断中的 UI Automation COM 引用干扰单独记录；[修复前后证据和范围](windows-native-performance.md) 不等于长期内存稳定或全部性能门禁完成。

- [x] 2026-09-16：完整 x64 原生应用真实合成语音上传/转写、问答/引用定位；视频发现并修复完成后副本入口需刷新及默认窗口转写不可见。修复后视频两项通过，最终核心/WPF 回归与双架构发布通过；范围及未验项见 [云端 UI 验证](windows-native-cloud-ui-validation.md)。

- [x] 建立功能矩阵：逐项记录 macOS 源码入口、实际能力、Windows 现状、实现位置、验收方式。见 [功能矩阵](windows-native-feature-matrix.md)，已核对相关 UI 测试入口；后续随实现更新。
- [ ] 核对后端契约：认证、上传、转写/摘要作业、实时字幕、库管理、搜索、问答、导出、版本历史。标注占位接口和服务限制，例如 macOS 录音重命名 Save 当前未接入 API。
- [ ] 完成原生窗口实验：托盘、可拖动录音条、独立字幕窗、普通库窗口；验证显示不抢焦点、控件可交互、置顶、关闭隐藏、恢复、DPI 和屏幕边界。
- [x] 完成最小应用 x64/ARM64 构建和发布；记录 WinUI 3/WPF 选型及依赖、维护成本、窗口能力依据。两候选均成功，采用 WPF；原始探针、双架构发布与依赖复核见 [构建和选型验收](windows-native-validation.md#2026-09-16阶段一构建和框架选型验收)。本项不包含 ARM64 实机、特殊窗口行为或安装验收。
- [ ] 记录性能基线：冷启动、托盘空闲 CPU、工作集/私有内存、安装与分发体积；统计全部应用相关进程与运行时依赖，明确测试环境。

阶段出口：功能矩阵可追踪；选定框架的真实窗口可操作、可构建，资源开销有测量依据。

## 阶段 2：录音核心与桌面生命周期

- [x] 2026-09-16 当前源码 Windows CLI 兼容补验：类型检查、226 项测试、构建、隔离安装/启动及严格包检查通过；x64/ARM64 helper 重建与包架构/许可证检查通过，x64 实际版本执行及 sidecar 握手通过。6 项平台/真实服务条件测试跳过，macOS、ARM64 实机与当前批真实采集未验，不据此勾选下面的复合录音验收项，见 [验证记录](windows-native-validation.md)。

- [ ] 建立原生应用和可测试的共享核心，隔离窗口/ViewModel、录音状态、网络服务和持久化。跨窗口共享一个录音会话，窗口销毁不能结束录音。
- [ ] 支持系统声音、指定进程及子进程、麦克风设备选择与开关；保留设备消失、权限拒绝、不支持进程录音系统版本的明确反馈。
- [ ] 对齐 idle/starting/recording/processing/done/error 状态；防止重复开始、开始未完成即停止、多窗口竞争和退出截断 WAV。
- [ ] 实现计时、录音电平、停止、丢弃确认、本地保存、打开目录；录音中切换麦克风不得破坏时间线。
- [ ] 实现托盘状态、显隐和恢复、单实例、退出录音确认；会话录音与后台处理独立于窗口生命周期。
- [ ] 实现音频活动/会议录音建议、静音或会议结束的停止提醒、隐藏后长录音提醒和时长上限；平台检测能力差异写入矩阵。
- [ ] 验证真实系统音频与麦克风混录、进程隔离、静音时间线、设备中断与停止落盘；运行现有 Windows CLI 回归。

阶段出口：无 Node、无 Web UI，真实录音闭环可用，关闭窗口仍安全录制。

## 阶段 3：账号、云处理和实时字幕

- [x] 2026-09-16 单句转写失败释放等待名额并继续后续字幕；半句和空结果在窗口/归档/TXT 中显式标记，停止尾句失败可排空。旧代码失败复现、核心 36 组、完整 WPF、受控生产字幕窗口视频三项及双架构发布通过。真实服务失败、长文本/多 DPI 与完整 App 异常流程仍待验，见 [验证记录](windows-native-validation.md)。

- [x] 2026-09-16 普通转写按音频提交确认记录句序，窗口/紧凑模式/历史淘汰与 TXT 导出不再按完成到达顺序排列；归档保留可选顺序信息，长导出分批磁盘排序。核心 35 组、完整 WPF 回归和双架构发布通过。当前限顺序追加提交链；任意前驱回填/插入、真实乱序服务视频与多 DPI 仍待验，见 [验证记录](windows-native-validation.md)。

- [x] 2026-09-16 字幕重连迟到结果按窗口状态版本隔离；Reset/连接/停止/新失败/关闭及当前失败十二种 WPF 回归、完整套件和双架构发布通过。真实网络恢复仍待验，见 [验证记录](windows-native-validation.md)。

- [ ] 原生首次引导、权限说明和设置入口；支持跳过登录本地录音，云功能按认证状态展示。
- [ ] 对齐现有登录流程、回调、刷新/过期/重连/退出状态；凭据保存在 Windows 受保护存储，不进日志或明文配置。
- [ ] 实现本地会话索引、上传进度、自动/手动转写和摘要、后台处理、状态轮询、失败重试与完成提醒。
- [ ] 实现实时字幕连接、原文、翻译、语言/场景配置、断线重连和失败提示；字幕失败不得中断本地录音，队列必须有界。
- [ ] 实现独立字幕窗展开/紧凑、原文/译文显隐、关闭与重新打开；窗口状态与录音会话一致。
- [ ] 验证离线录音、上传失败保留本地文件、会话过期、字幕断网恢复、后台处理时新建录音；真实服务测试仅使用当前已授权的测试账户和可控样本。

阶段出口：真实录音能上传、生成内容并恢复失败；字幕不影响录音可靠性。

## 阶段 4：录音库、回顾与设置

- [ ] 现代 Windows 视觉专项：按用户 2026-09-15 反馈重做视觉，不以 WPF Fluent 主题启用作为完成依据。录音库/录音条第一轮自定义样式和浅色实窗检查已做；全窗口统一、浅深色、最小尺寸、空/错误/选中状态及键盘焦点验收仍未完成。

- [ ] 按 macOS 源码核对并调整整体流程、窗口/页面布局与 UX：录音入口和状态切换、统一录音库导航、列表与详情布局、字幕窗、设置分组。逐屏记录差异与实际验证；原生控件风格差异可接受，主要流程与布局偏差必须说明或修正。

- [ ] 录音列表、加载/空/错误状态、刷新、搜索、选择恢复、当前会议入口、音频文件导入及处理。
- [ ] 摘要与逐字稿、长文档虚拟化、说话人展示/已支持的修改、文字复制、音频播放/跳转/倍速、文字与播放定位联动。
- [ ] 问答输入、流式回答、错误重试、引用跳转；避免切换会议时串用对话或播放状态。
- [ ] 对齐实际支持的重新转写/摘要、失败分块重试、历史版本、本地同步、导出、浏览器打开、删除确认与失败恢复。
- [x] N23 本机移除实现与自动回归：独立 `library-removed.json` 标记隐藏条目，保留音频/字幕/元数据及处理记录，已提交云任务继续运行；确认默认取消。WPF 验证取消、选择变化、读写失败保留列表和播放、成功释放音频句柄、文件不变、重新实例化存储与迟到保存不复活；核心 36 组和完整 WPF 通过，双架构发布完成，范围见验证记录。
- [ ] N23 本机移除实机验收：完整 App 确认框、取消/成功、刷新与新进程恢复、已提交云处理并行和关联显示、键盘/尺寸/DPI；自动回归不替代这些操作。
- [x] N23 限定实操补验：完整 x64 App 确认内容、目标移除保留其他录音、新进程列表恢复视频三项通过，6 个原文件 SHA256 保持；取消到再次确认连续视频证据不足。实操发现系统确认位置偏出库窗口，已改为 CenterOwner 原生窗并补实际 ShowDialog 回归、完整 WPF 和双架构发布。见 `local-removal-ui-01` 验证记录，不补勾全项。
- [x] N23 统一库关联回归：合并条目移除本机副本后保留云端条目/选择，隐藏本机详情和副本入口；迟到云完成通知及刷新不恢复本机副本，零 DELETE。实际 WPF 与 HTTP 替身通过，不替代真实云后台并行。
- [x] N23 恢复原处理身份：重新导入原目录的已移除音频恢复原 ID/标题/选项/字幕，读写失败或取消不产生新条目。失败上传移除后恢复并继续原 ticket 的 HTTP 替身回归通过，避免创建第二条云录音；真实服务与完整 App 恢复实操仍待验。
- [ ] 设置覆盖主题、账号、权限、录音设备、字幕/翻译、录音建议与提醒、时长上限、存储目录和关于/更新；按平台采用等价原生行为。
- [ ] 更新机制使用可验证的发布来源；如生产签名/发布权限暂缺，完成可测试实现并明确外部阻塞，不伪造已发布。

阶段出口：矩阵中每个 macOS 已实现功能都有 Windows 对应操作及验证，服务端未支持项单独列出。

## 阶段 5：质量、轻量化与交付

- [x] 2026-09-16 完整发布 x64 App 受控进程录音可见/隐藏各约 81 秒及停止后约 41 秒采样；CPU 单核 2.176% / 1.704% / 0.077%，228.142 秒 WAV 完整与内部信号检查通过。仅短时、无字幕场景，长期/录音加字幕与完整门禁保持未完成，见 [性能报告](windows-native-performance.md)。

- [ ] 按 Microsoft Store 主分发渠道完成 MSIX 交付；以 ScreenCam 实际发布脚本、依赖和生命周期门禁为参考，保持 Recappi 的 WPF、无 WebView/Node 约束及 x64/ARM64 范围。详见 [商店发布与体积优化计划](windows-native-store-release.md)。
- [ ] 审计自包含包冗余依赖，逐项验证优化；提交同版本、同架构的优化前后下载字节、安装占用及冷/热启动对比。商店共享依赖、差分下载的收益必须独立实测，不能用 ZIP 大小替代。
- [ ] 在无 SDK、无预装应用运行时和历史 Recappi 数据的干净系统验证安装、首次启动、真实录音/播放/导出、升级保留数据、卸载及重装；保留安装日志、依赖清单、视频和关键帧判定。开发机启动不能替代此门禁。
- [ ] 测试关键状态转换、会话/设备/网络失败、凭据隔离和云契约；加入可重复的原生 UI 冒烟测试。
- [ ] Windows 实机验证托盘恢复、关闭/退出、键盘访问、缩放、多屏和长逐字稿。无法在当前硬件验证的项目明确列出，不以模拟代替。
- [ ] 分别测量托盘空闲、录音、录音加字幕、长逐字稿和关窗后的 CPU/内存；关闭不需要的计时器、动画与窗口，排查持续增长和后台忙轮询。
- [ ] 完成 x64/ARM64 发布产物、干净环境启动验证、运行时依赖说明、安装/卸载与用户数据保留策略；ARM64 编译和 ARM64 实机验证分别记录。
- [ ] 运行受影响的 CLI、原生核心与 macOS 可用检查，确保旧入口兼容。最终整理功能矩阵、验证命令/结果、性能数据和已知限制。
- [x] 按仓库流程提交功能分支并创建 [PR #26](https://github.com/Recappi/recappi-mini/pull/26)，后按用户直接合入默认分支的明确授权于 `8ac09de` 合并至 `main`。实机/性能未完成项保留；代码合并不代表其他阶段验收完成。

## Goal 完成标准

完成以上实现与可执行验证，交付原生应用产物及可审查变更。仅有计划、空窗口、模拟数据或主流程演示不算完成；未验证或受外部条件阻塞的项目不得勾选。按依赖顺序推进，优先修复录音数据完整性和生命周期问题。

## 选型参考

- Microsoft Windows 原生开发路径：https://learn.microsoft.com/en-us/windows/apps/get-started/
- WPF 概览：https://learn.microsoft.com/en-us/dotnet/desktop/wpf/overview/

## 执行记录

已验证的增量检查点（不替代上述阶段验收）：

- [x] 2026-09-16：修复取消登录后迟到授权仍保存新账号的竞争。四种首次/已有账号与内部/外部取消组合先复现失败、修复后原凭据字节和状态保留且下次登录成功；核心 34 组、完整 WPF 及双架构自包含发布通过。响应流时序为受控测试，真实浏览器取消仍待验，见验证记录。

- [x] 2026-09-16：完整 x64 App 保存说话人默认资料、重新打开/取消、新进程恢复图标标签实操三项通过；两段 255.9 秒视频、2559 帧→23 关键帧，见 `speaker-save-cloud-ui-01/acceptance.html` 和验证记录。实际姓名输入仍受当前工具路径限制，修改后保存保持证据不足；默认资料不替代文字编辑验收。云样本与临时凭据已清理，应用已退出。

- [x] 2026-09-16：完整 x64 App 真实合成样本的 4 条推荐显示、选择填入、发送及回答完成视频三项通过；`ask-suggestions-cloud-ui-01/acceptance.html`，58.8 秒／588 帧→7 关键帧。真实草稿时序仍证据不足，不补勾；样本与临时凭据已清理、本机音频保留。见云端 UI 验证，其他键盘/主题/失败分支保持待验。

- [x] 2026-09-16：真实服务验证同账号同时启动两个本地任务均上传成功，其中一条完成转写、下载、问答和 4 条推荐解析；两条本轮云样本清理成功，本机两份音频及下载副本 SHA256 与源一致。报告 `core-tests-72b9576f519b4836a5bacaa1b0fa7c5b/cloud-pipeline-smoke.json`，细节见验证记录；不是完整 App 视频或失败恢复验收，不量化真实任务重叠时间。

- [x] 2026-09-16：按后端真实路由的单账号上传限制修复本进程并发上传冲突；创建至 complete 按账号排队，转写期间允许下一条上传。新增回归先复现 409，再覆盖转写重叠及整批取消/按原上传优先恢复；取消释放许可竞争亦已修复。核心 33 组通过，具体范围见契约对照；真实完整 App 双上传、旧暂停任务及其他客户端占用的恢复仍待验，不补勾整个后台处理阶段。

- [x] 2026-09-16：读取后端固定提交 `4c3eeb7`，发现推荐问题实际返回对象数组；C# 兼容对象/旧字符串并过滤无效条目，回归先复现 JsonException，修复后核心 32 组与完整 WPF 通过，含推荐显示及选择填入问题。双架构发布报告 `build/native-desktop-release/8262508a43364af9a41491e2fe818826/release-report.json`（`86aabe9` 加本轮修复，dirty）。未做真实推荐 UI 视频，不作线上部署版本或完整 UI 验收结论；后端单账号上传限制与客户端两个处理槽位的并发适配仍待核验，详见契约对照。

- [x] 2026-09-16：按 `43eb550` 的 C#/macOS/CLI 源码建立 [逐接口契约对照](windows-native-cloud-contracts.md)，区分真实请求、解码兼容、测试样本与服务证据。纠正接口范围容易误读为 C# 支持 abort 的描述；记录转写 provider/推荐语言默认值、上传 ticket 保留期尚未确认。只完成客户端源码核对，部署后端契约、设备登录及异常实操仍待验，不勾选整个契约阶段。

- [x] 2026-09-16：干净提交 `bf3ccd2`（包含 Ask 草稿、推荐加载与删除确认修复）重新发布完整自包含 x64/ARM64 ZIP，分别 76,836,291 / 71,234,374 B；发布报告 `build/native-desktop-release/6662a547382e4c6c8e0ab6f1770535bb/release-report.json`。双架构未签名 MSIX SDK 校验和 479/478 个载荷文件长度及 SHA256 对照通过，报告目录分别为 `build/native-msix/eef63928a3e64f93a522e12029269199` 与 `build/native-msix/59f20f963a024c5e8466b90b0d47d3eb`。32 组核心测试通过（`core-tests-cda8848e46204e748648c99cefa55589`）；同一代码上一轮完整 WPF 回归通过。x64 隔离 PATH 下五次启动和正常退出通过，单独测量中位 792.69 ms，详见性能报告。未签名、未安装，ARM64 未运行，干净系统与冷启动仍待验；本轮没有新增 UI 视频验收。

- [x] 2026-09-16：修复重新转写/摘要确认期间目标变化导致误提交的问题。新增 WPF 回归先复现“确认 r1、切换后 POST 到 r2”；现在确认前后校验选择代次、已登录账号及最新按钮资格，所有处理提交也核对当前账号状态。嵌套 Dispatcher 回归覆盖两个操作在切换录音、退出账号、刷新发现活跃任务期间均零提交，并验证随后正常请求仍可执行；完整 WPF 回归通过。使用测试 HTTP 与确认委托，不替代真实确认框视频或服务端重新处理验收。

- [x] 2026-09-16：完整 x64 App 录制独立 443 Hz 测试进程期间，成功将 344,649,225 B 合成 MP3 导入为 2,757,189,214 B WAV；导入完成自动选中，当前录音继续，手动停止后独立保存 Done / 143.380 秒。视频 `build/native-desktop-validation/import-recording-ui-01/acceptance.html` 三项通过，240 秒／2400 帧→14 关键帧、零越界事件。`verification.json` 核对两个结果、源哈希不变、录音六个时点均为 443 Hz，排除首尾各 0.5 秒后 1423 个 100 ms 窗口无整段静音；不证明逐样本无缝或听感。应用、播放器和录屏均已退出。复用 `1089045cc2fd49cd9851993f1cadf0ec` 产物，未改生产代码；录屏仅覆盖录音库，不验文件对话框、独立录音条、云处理并行或麦克风混录。

- [x] 2026-09-16：所有托盘提示统一经 RecordingNotifications 呈现；普通完成/失败/提醒通知清除旧录音建议，来源刷新期间的新通知或抑制使旧选择失效，刷新失败不从旧列表选择。新增 RecordingNotificationTests 使用实际 RecorderWindow/来源控件与可控通知、枚举等待，验证有效点击仅选择不采集、普通通知覆盖、延迟刷新、清除、来源消失及呈现失败；完整 WPF 回归通过。实际 x64 App 播放独立 443 Hz 进程后，Windows 显示指向测试 PID 的建议，应用保持空闲、未创建录音；视频 `build/native-desktop-validation/notification-ui-01/acceptance.html` 一项通过／一项证据不足（75.9 秒、759 帧→6 关键帧、零越界事件）。工具未提供可操作的系统通知窗口，实际点击/替换及通知中心历史条目行为仍待验，不以回归替代。双架构发布 `build/native-desktop-release/1089045cc2fd49cd9851993f1cadf0ec/release-report.json`，10f0bd6 加本轮生产改动；x64/ARM64 ZIP 70,968,106 / 65,366,113 B。测试进程正常退出，无真实账号/录音数据。

- [x] 2026-09-16：完整 x64 App 的一分钟时长提醒→继续录音→手动停止保存实操通过。仅捕获独立 443 Hz 测试播放器进程，未登录/未启用麦克风或云处理；提醒出现后未自动停止，继续操作收起提醒且保持同一录音，随后保存 Done / 117.496875 秒 WAV。视频 `build/native-desktop-validation/duration-reminder-ui-01/acceptance.html` 三项通过，177.7 秒／1777 帧→41 关键帧，零越界事件。`verification.json` 核对 48 kHz 单声道 PCM16、时长/ID、三个时点的 443 Hz，以及排除首尾各 0.5 秒后的 1164 个 100 ms 窗口无整段静音；不证明逐样本无缝或听感。`cleanup.json` 验证应用、播放器与录屏正常退出。复用 1eeaa4a1ceb84bfba1a2a052f56ec627 产物，生产代码未改；隐藏通知、静音提醒、提醒内停止按钮、多屏与真实长会仍待验。

- [x] 2026-09-16：干净 8f40070 的 CLI 类型检查、226 项测试、打包/隔离 consumer 安装、x64 helper 实际启动与 sidecar 握手/音源枚举通过；双架构 helper 构建及 tarball 架构/许可证检查通过。6 项平台/真实服务测试明确跳过；ARM64 未运行。当前原生载荷的双架构 MSIX SDK 与文件哈希复验通过，仍未签名/安装。具体命令、环境和范围见 [验证记录](windows-native-validation.md#2026-09-16当前提交的-cli-兼容与双架构包检查) 与 [MSIX 记录](windows-native-store-release.md#2026-09-16当前代码的-msix-载荷复验)。本轮未改生产代码。

- [x] 2026-09-16：修复本机播放计时超过一小时后省略小时的问题；暂停跳转与播放计时共用总小时格式，一小时内保留分秒。真实 3661 秒 WAV 回归先复现 `3605 → 00:05`，修复后验证 `3599 → 59:59`、`3605 → 01:00:05`、播放计时和回退短时间；完整 WPF 回归通过。完整 x64 App 视频两项通过／一项证据不足，`build/native-desktop-validation/long-playback-ui-01/acceptance.html`：75.3 秒、753 帧→7 关键帧；最后回退操作的事件越界，未判视频通过。样本为隔离库内 61 分 1 秒静音 WAV，不证明真实长时录音或听感。双架构 self-contained 发布报告 `build/native-desktop-release/1eeaa4a1ceb84bfba1a2a052f56ec627/release-report.json`，基于 898c38a 加本轮代码；x64/ARM64 ZIP 70,967,623 / 65,365,618 B。

- [x] 2026-09-16：完整 x64 发布应用真实大文件转码中取消及取消后重试通过。合成 MP3 344,649,225 B，ffprobe 实测 28,720.718 秒；取消前已生成 120,960,046 B 临时 WAV，取消后录音目录为空、源文件 SHA256 不变。随后短 MP3 导入成功，目录仅一个 Done / 9484 ms 副本，取消目录未重新出现。视频 `build/native-desktop-validation/import-cancel-ui-01/acceptance.html` 两项通过，207.9 秒／2079 帧→17 关键帧，零越界事件；磁盘证据为同目录 `partial-before-cancel.json`、`cancel-verification.json`、`verification.json`。应用与录屏进程正常退出。本轮复用 ef06961 对应的 3e7758bb8651430caf5da61d78a1d035 发布产物，未改生产代码或重复构建。10 fps 无声录像不证明听感或精确取消延迟；完整大文件导入完成、云详情期间取消实操及其他格式/尺寸仍待验。

- [x] 2026-09-16：统一录音库侧栏接收导入进度/结果与取消入口，选择云端详情不再隐藏取消操作；文件选择与转码期间禁止重复启动。新增 ImportLifecycleTests 验证重复对话框/任务抑制、切云详情取消可达、取消/失败后恢复、过期进度丢弃、真实 WAV 解码选择及关窗取消；完整 WPF 回归通过。实际发布 App 无效 WAV 失败→MP3 重试→播放视频三项通过，`build/native-desktop-validation/import-recovery-ui-01/acceptance.html`（113.5 秒、1135 帧→17 关键帧、零越界事件）；`verification.json` 验证仅一个 Done 副本、48 kHz 单声道 PCM 9.485 秒与源文件保留。双架构 self-contained 发布报告 `build/native-desktop-release/3e7758bb8651430caf5da61d78a1d035/release-report.json`，基于 40506da 加本轮改动；x64/ARM64 ZIP 70,967,520 / 65,365,516 B。无声录像不证明听感，大文件真实取消、云选择期间实操和全部尺寸仍未验收。

- [x] 2026-09-16：完整 x64 自包含应用未登录导入合成 WAV，实际文件对话框 Escape 取消、重新选择后自动选中、播放前进与结束复位视频三项通过。`build/native-desktop-validation/import-ui-01/acceptance.html`：105.3 秒／1053 帧→14 关键帧，事件均在录像内；`verification.json` 核对 Done、9417 ms、48 kHz 单声道 PCM、源文件保留及进程退出。使用 c4424c3a37db4c6ebc99d37d5b887116 发布应用；本轮未改生产代码，不重复构建。无声录像不证明听感，其他格式、大文件、转码中取消、重启和云上传仍待验。

- [x] 完整 x64 应用的原始字幕 JSONL 导出实操：保存类型从文本切到归档时文件名自动变为 `.jsonl`；目标 251 B、2 行，与原归档 SHA256 完全相同。两段 10 fps 视频 1639 帧→8 关键帧，画面分析与自动证据检查 2 项通过/1 项证据不足。报告 `build/native-desktop-validation/caption-archive-video-3ba2c6eb1d0a4bc4ae3e3d9bdb8bac94/acceptance.html`；合成字幕无音频，覆盖确认/选择竞争仍待验。录制关闭窗口后结束，末黑帧不证明应用闪烁；同时修正分析脚本写死 15 fps 的说明。

- CI 兼容性跟进：`b516317` 的运行 `34992471446` 在 macOS 字幕非终止关闭码重连测试失败（200 ms 后 claim 次数仍为 1，要求 ≥2）；不能据此确定生产重连故障。该测试改为单调时钟下最多等待 2 s、观察真实 claim 次数，保留原断言与停止清理。当前 Windows 无 Swift/macOS 测试环境，仅完成差异检查，修正是否消除失败须由后续 macOS CI 确认。

- [x] 处理记录损坏/暂时不可读使用明确恢复提示，分别引导先核对云端或稍后重试，说明本地音频保留且不会重新上传；库视图和后台通知接入，旧账号/录音的延迟异常不再覆盖当前库状态。新增 WPF 回归实际打开保留的本地 WAV，验证损坏提示、零云请求、音频/记录不变；完整 WPF 回归及双架构发布通过。使用测试认证与合成音频，实际服务/系统通知/视频仍待验。

- [x] 修复恢复上传把损坏/锁定处理记录当作不存在而重新请求云端的问题：目标记录独立读取并核对身份/状态，异常时不发请求、不覆盖原记录；列表读取允许原子替换。录音元数据实测出现同类文件拒绝，设置/上传记录/录音元数据统一有界替换与失败清理。核心 32 组、完整 WPF 回归通过，核心证据 `core-tests-c994b138343345359db53705efe1a175`；真实云/UI 失败恢复仍待验。

- [x] 完整 x64 发布应用的合成双语字幕文本导出、Escape 取消及录音目录目标拒绝实际操作完成；三段视频 4412 帧→21 关键帧，自动证据门禁与画面分析 3 项通过/2 项证据不足。两次 74 B 文本内容相同、原字幕两行保留，两个测试进程正常退出。首段遗漏独立对话框且末事件越界，补录区域有缩放裁剪，未宣称完整连续布局；JSONL/覆盖确认/切换竞争与真实音频链路仍待验。报告 `build/native-desktop-validation/caption-export-video-53b88d50a26241d6930ebe5152bafd81/acceptance.html`。

- [x] 真实文件锁复现云处理记录替换失败，增加仅限 Windows 文件冲突的 25/50/100 ms 有界重试与临时文件清理；短暂占用后上传完成、永久占用保留原记录、释放后重试不重复创建/转写均通过。核心 31 组通过，专项证据 `core-tests-c40bf4e71dce46c2aa98f64340cd689a`，全量 `core-tests-7ea4dbf806d84f77aeb7e64373032d7f`（位于 `build/native-desktop-validation/`）。确认该存储失败路径，不据此断言上轮偶发失败的占用来源；云端响应为测试服务，真实云交互视频仍待验收。

- [x] 本地字幕导出改为同目录临时文件完整写入后替换，拒绝录音存储目录内目标，并在保存对话框返回后核对选择；专项验证缺失/锁定来源、锁定目标保留已有文件、临时清理及双语/原始归档内容通过，完整 WPF 回归通过。真实对话框视频尚未补验。本轮完整核心回归在既有云上传测试失败，状态为 `Failed / UploadCompleted=true / TranscriptionAttempted=false`，未证明意外转写；需定位失败原因，不宣称核心全通过。专项证据 `core-tests-ca15cb9c203e42df96348869af86b236`，失败证据 `core-tests-2eeaf341b0f84a0d9821083d8ce823ba`（均在 `build/native-desktop-validation/`）。

- [x] 显式在线 EXE 候选约 2.50/2.49 MB，运行时探针和固定 Microsoft 下载/校验/授权后重验流程接入；双架构构建、x64 已有运行时隔离安装→启动退出→卸载保留用户文件，以及注入缺失运行时静默拒绝通过。缺失时另需 60.03/54.03 MB；真实干净环境补装/UAC/取消、在线升级、ARM64 实机与签名仍未验，MSIX 策略未替换。

- [x] 增加显式共享 .NET 运行时候选发布：双架构应用 ZIP 约 0.57/0.56 MB，依赖与候选身份写入报告；本机 x64 就绪/正常退出及注入缺失框架负向检查通过。现有 EXE/MSIX 拒绝未附带运行时的载荷，默认自包含构建/EXE 编译保持通过。自动补装、干净环境、ARM64 实机未完成，不能把小包大小当首次下载总量；见商店发布与体积记录。

- [x] 完整 x64 应用免登录四步引导/返回、深色与提醒设置保存、新进程恢复后的设置视频检查完成；三段 3117 帧→32 关键帧，5 项通过/2 项证据不足，两个测试进程正常退出。启动/退出过渡未连续录制，全部设置、真实登录/权限、多屏/DPI 与 ARM64 仍待验；见验证记录。

- [x] 桌面进程录音增加 15 秒异步激活期限与迟到资源回收，核心 29 组、CLI 23 项和完整 WPF 回归通过；真实进程音源连续两次录音保存通过。引导压力复现设置替换的短暂拒绝访问，增加有界重试后真实文件锁测试及 50 轮引导通过。双架构 ZIP/未签名 MSIX 与 x64 无 Node 启动退出验证完成，详见验证记录；同步原生调用阻塞、完整 UI 视频、签名安装和 ARM64 实机仍待验。

- [x] 同一干净提交 7b2272a 的完整/精简双架构 ZIP、未签名 MSIX 与文件差异门禁通过；x64/ARM64 MSIX 文件长减少 7.91%/8.46%。x64 交替 5 对启动均就绪并正常退出，中位 760.16/779.37 ms；暖缓存、小样本不证明提速。实际安装占用/Store 下载、资源回退、冷启动和 ARM64 实机仍待验。

- [x] 修复关闭麦克风后旧电平阻止静音提醒、UI 节流吞掉清零的问题；核心 27 组与完整 WPF 回归通过。当前 x64 发布 App 用真实进程音源和 Steam 虚拟麦克风完成开关/重开/保存 96.96 秒，视频 1689 帧→35 关键帧、3 项通过/2 项证据不足；双架构 ZIP/MSIX 校验通过。物理混录/拔插、真实静音提醒、签名安装与 ARM64 实机仍待验。

- [x] 实际双语字幕录像发现并修复共用滚动区遮挡：展开独立双栏、紧凑双行、单路占满与至少一路保护；完整 WPF 回归通过。四段视频 6676 帧→164 关键帧，5 项通过/3 项证据不足；双架构 ZIP 和未签名 MSIX 校验通过。历史滚动/模式切换连续视频、其他 DPI/主题、签名安装和 ARM64 实机仍待验，详见验证记录。

- [x] 真实回环 WebSocket 断连/延迟重连测试通过：录音 WAV 在握手等待期间持续增长，跨连接字幕和停止尾句保留，分片中文正确还原；Release 核心 26 组通过。使用合成 PCM/协议服务，不替代真实设备、公网断网/过期或视频 UI 验收。

- [x] 真实 WASAPI/en→zh 字幕双窗对照确认界面检查连接影响 CPU：连接/断开/重连/再断开为 53.99%/3.33%/47.71%/3.33% 单核；新增线程 CPU 与输入源存活采样门禁。两次 WAV/字幕归档通过、临时账号清理；仅定位本轮测量干扰，未改生产可访问性，完整性能和视频 UI 验收仍未完成。

- [x] 完整 App 实际 WASAPI 进程录音强制结束后，重启恢复 93.723 秒音频，已落盘载荷 SHA256 不变；四段视频 4593 帧→48 关键帧，7 项通过/2 项证据不足。录像发现并修复暂停 seek 时间滞后，WPF 18 组、双架构包及修复后拖动/Home/End/播放结束视频通过；掉电、混录、云字幕恢复和完整视觉仍待验收。

- [x] 启动时识别异常中断的本机会话，校正自有 WAV 长度/时长、保留载荷和字幕、标明中断并允许检查播放；不自动上传。核心 25 组、WPF 17 组及完整 x64 App 隔离恢复门禁通过，双架构包已重建；合成输入子进程故障测试不替代真实设备、掉电或视频 UI 验收。

- [x] 录音选项固定底部操作、配置区域独立滚动和初始名称焦点；500/360 高度几何及完整 WPF 回归通过。真实包身份下视频 1613 帧→12 关键帧，4 项通过/2 项证据不足，验证退出可见、反向键盘定位、字段可达和空闲退出后进程结束；录音中退出与其他 DPI 仍待验证。

- [x] AUMID 激活开发注册应用并核实真实包身份；独立设置页视频 806 帧→4 关键帧，3 项通过/2 项证据不足。精确开发注册已清理，无证书信任修改；签名安装、正常退出、录音/升级与干净环境门禁仍未完成。

- [x] 安装方式决定更新入口：MSIX 使用原渠道/Store，未知身份禁止便携 ZIP 更新；注入身份及便携回归通过，双架构精简/MSIX 重建通过。真实安装后的包身份、按钮视频与升级行为仍待验收。

- [x] 首轮 x64/ARM64 未签名开发 MSIX 构建，Windows SDK 校验及全部载荷长度/SHA256 对照通过；精简候选双架构资源审计通过。尚无签名安装、商店认证、干净系统或 ARM64 运行证据，详见商店发布计划。

- [x] 首轮完整发布应用录音库视频验收：两段 15 fps 录像共 3923 帧，提取 20 个关键帧并完成 AI 画面分析及证据门禁，6 项通过、2 项证据不足；只覆盖当前浅色本机库的布局、选择、播放/暂停和搜索，不替代全窗口及启动验收。

- [x] 修复保存对话框期间切换录音/账号造成的导出内容混用：固定文字标题、正文和音频来源，确认后重新核对；取消或来源变化保留目标文件。新增回归先复现失败，再完成 WPF 17 组与双架构发布检查；真实文件对话框操作仍待验。

- [x] 增加合成输入下录音/字幕生产窗口的可重复隔离诊断，完成默认/软件渲染、单窗/双窗/隐藏/文本静止对照；发现首次显示与恢复后差异，不能据单次样本归因控件或 GPU。完整 App 高 CPU 根因与性能达标仍未完成，见性能报告。

- [x] 完整 x64 发布 App 的受控进程 WASAPI→真实 en/zh 字幕→本地 WAV/最终归档通过，隐藏后继续，隔离账号副本已清理。可见/隐藏性能采样已记录；可见高 CPU、首屏空白截图、断网/过期重登与混录仍待定位/验收。

- [x] 完整 x64 自包含应用 5 次新进程首帧/设备/账号恢复/开始按钮就绪及正常退出验证，隔离 PATH 无 Node；中位约 757 ms。双架构包已更新。OS 冷缓存、首次引导/联网账号启动与全部运行场景性能仍待验收。

- [x] 普通窗口/录音条按实际显示器工作区恢复位置，17 组 WPF 回归通过；完整 App 实际拖动到边缘、移动后隐藏与同实例恢复保留位置已验。物理多屏、DPI 切换、拔屏与超大窗口布局仍待验收。

- [x] 原生账号套餐/用量与可信管理入口，核心 24 组/WPF 16 组通过；已授权测试账号真实只读用量接口解析通过，测试数据浅深色窗口已查看。真实管理服务、完整账号 UI/键盘/小屏验收仍待完成；矩阵同步为当前状态。

- [x] 面板退出入口共用托盘退出流程；真实 WASAPI 进程录音中取消退出继续录音、再次确认保存并退出通过，129.927 秒 WAV 结构和会话状态验证通过；15 组 WPF 回归通过。并发退出、账号/字幕竞争及键盘专项仍待验收。

- [x] 字幕启动屏障及启动前停止/终止、首条消息归档回归；归档故障持续提示与模拟输入下 WAV 保存验证。核心 23 组/WPF 15 组通过，完整 App 真实服务与竞争场景仍待验收。

- [x] 紧凑录音条无系统标题栏、独立移动手柄及品牌 ICO/托盘图标；15 组 WPF 回归与真实选项/Esc/隐藏恢复通过，新 x64/ARM64 包及 x64 实际外观已验。跨屏拖动、DPI 与 ARM64 实机仍待验收。

- [x] 共用视觉样式扩展到设置/字幕/账号/引导等窗口；设置浅深色切换、滚动、深色库搜索定位实测，15 组 WPF 回归通过。新 x64/ARM64 自包含包及 x64 实际启动/待机采样完成。完整视觉、ARM64 实机、安装器和最终验收仍未完成。

- [x] 重新登录后同一录音的字幕恢复编排与归档续写已实现；尾行隔离、原内容保留和异步重复点击回归通过。完整 App 的过期→登录→恢复、账号/退出竞争仍待端到端验收。

- [x] 字幕失败手动重连入口，复用流与归档、保留既有文字、重复请求/停止后重连保护；核心 23 组/WPF 15 组通过。真实恢复及账号过期终止流后的重新登录恢复仍待完成。

- [x] 字幕 WebSocket 握手保留 HTTP 错误状态；本机真实握手 401/403/503 与字幕授权拒绝不重试回归通过，核心 23 组/WPF 15 组通过。真实服务拒绝与重新开启流程仍待验收。

- [x] 原生业务 HTTP 401 同步账号过期，核对账号与凭据并原子更新；延迟旧凭据不覆盖新登录/退出。核心 22 组、WPF 15 组通过，过期清云内容且保留本机详情；真实过期与 WebSocket 认证拒绝仍待验收。

- [x] 录音库侧栏账号身份/连接状态入口；已连接、离线、退出身份清理与入口回调回归，真实未登录头部布局已验。真实过期/账号切换及键盘完整流程仍待验收。

- [x] 当前 x64/ARM64 自包含便携包重建；真实 x64 App 未登录、建议关闭时可见/托盘隐藏各约 37 秒 CPU/内存采样。报告明确环境、后代进程观察和局限；冷启动、建议开启、录音/字幕、ARM64 实机与安装器仍待验收。

- [x] 统一列表实际分组虚拟化修复；10,000 条生产窗口合成数据测量、分页/定位/跨日/关联性能报告及 500 条常规回归。见 [性能报告](windows-native-performance.md)；完整 App 空闲、录音/字幕与真实大库性能仍未验收。

- [x] 本机/云端单一日期列表，按当前账号已确认上传关联合并副本；保留本机音频与云端内容入口，搜索同步去重。核心 21 组、WPF 14 组通过，包含关联歧义/账号隔离/选择保留/删除云端保留本机。真实窗口已验本机今天分组与选择；真实账号合并、离线切换、分页及大库性能仍待验收。
- [x] 云端时间兼容与跨日定时/激活刷新，可控日期回归验证跨日、数日恢复、日期回拨与选择保留；真实午夜/休眠及多时区仍待验收。

- [x] 统一搜索真实窗口输入、本机结果定位及键盘清空查询恢复列表已验证；未登录可用。云端片段定位、全部键盘导航仍待实际验证。

- [x] 主窗口统一搜索本机标题与当前账号云端缓存；查询取消、旧结果抑制和退出隔离回归通过。真实窗口输入/定位及未缓存云内容覆盖尚未验收。

- [x] 录音库当前会议入口与已有录音状态接入；活动名称/时长、返回录音控制、结束清除的 WPF 回归通过。真实跨窗口恢复与当前会议完整详情仍待验收。

- [x] 本机与云端库入口汇入单窗口，共用侧栏与详情区域；未登录本机播放/暂停实测、互斥选择及退出账号隔离回归通过。日期分组、当前会议、副本合并和统一搜索仍待对齐。

- [x] 按 macOS 状态分工重排紧凑录音面板、原生选项弹层、录音控制及保存结果行；真实受控进程录音 14 秒、停止及查看定位通过，WPF 回归通过。全部选项、云处理进度与统一库导航未完成。

- [x] 录音来源/麦克风失效不静默替换：保留选择、明确提示、禁止开始，支持显式恢复；WPF 回归及真实窗口失效来源刷新通过。录音中设备中断仍待完整验收。

- [x] 设置即时保存与逐字段并发保护：开关/选择立即应用，文字延迟保存，失败可重试，关闭提交未完成输入。WPF 回归与真实窗口切换主题持久化通过；权限、账号和其他页面 UX 仍待对齐。

- [x] 按 macOS 四步流程新增独立首次引导、步骤持久化/恢复/跳过/返回/再次引导；设置按五个标签页分组。WPF 回归及真实窗口免登录完成流程通过。实时权限状态、登录体验、设置即时生效与其他页面布局仍待对齐，阶段验收保持未完成。

- [x] 当前用户安装器及 x64/ARM64 构建；x64 实际安装启动、运行中拒绝升级、原生文件写入失败回滚、升级、防降级与卸载保留用户文件测试通过。仍缺签名、干净环境、ARM64 实机和自动安装更新；不等同完整交付验收。

- [x] 原生官方发布源检查、架构/稳定与预览版本筛选、下载完整性校验、取消/失败保留原文件及设置入口；核心 20 组与 WPF 回归通过。真实源暂未提供匹配的可验证 Windows 包；自动安装、签名和升级回滚仍未完成。

- [x] 当前 x64 发布版实际启动、主题保存/恢复与关闭后重复启动恢复同窗口；测得可见空闲 CPU 偏高（约 14.9% 单核），隐藏后约 0.83%，需继续定位，性能阶段保持未完成。

- [x] 当前源码 x64/ARM64 自包含便携 ZIP 发布、锁定依赖恢复、架构/运行时/压缩包校验及 SHA256 报告。安装器、签名与该批实际启动仍待验收。

- [x] 应用声音建议策略与设置、实际峰值采样入口及托盘选择来源接入；debounce/每活动一次/隐藏/关闭建议回归。真实提示点击、会议识别及空闲负载仍待验收。

- [x] 系统/浅色/深色主题保存与应用、实际版本/架构关于信息；WPF 暗亮输入框前景/背景变化及重读设置回归。全窗口视觉、系统主题变化和高对比度仍待验收。

- [x] 已保存账号的离线恢复、缓存列表/详情与离线搜索入口；WPF 断网恢复回归验证不继续请求云详情、问答、历史或音频。真实断网/重连操作仍待验收。

- [x] 按账号加密缓存跨录音标题/摘要/逐字稿搜索、原始说话人过滤、损坏缓存隔离、取消；WPF 搜索展示、断网时标记缓存回退、退出账号清理。完整结果点击定位、重启离线入口及大库性能仍待验收。

- [x] 说话人姓名/图标/备注的原生编辑、本机加密持久化及账号/录音隔离；核心及实际 WPF 弹窗保存回归。完整键鼠、取消/切账号弹窗流程仍待验收。

- [x] 播放位置驱动逐字稿活动片段；实际 WPF 高亮样式、时间边界和真实播放器 seek/清理通知回归。

- [x] 逐字稿保留时间/说话人及旧格式兼容，10,000 片段 WPF 虚拟化、当前内容/说话人筛选、引用定位和清理回归。全库缓存搜索、说话人重命名及播放自动高亮仍待实现。

- [x] 本地音频导入：Windows 原生解码 WAV/MP3/M4A、取消清理、原文件保留；WPF 导入后列表选中及播放入口回归。文件选择对话框完整操作仍待验证。

- [x] 原生 C# 字幕真实服务合成语音：普通转写收到最终原文，英→中模式收到原文/译文。
- [x] 设置重启读取、非法保存保护、每会话转写选项快照；WPF 设置控件保存验证。
- [x] 双流字幕完整归档超过窗口历史上限、损坏末行读取、归档丢弃清理；只上传后继续转写不重复上传。
- [x] 录音提醒策略边界、继续录音状态、WASAPI/进程活动枚举；完整应用设置保存和关闭后重复启动恢复同一窗口。
- [x] 原生 C# 真实合成语音上传→转写→下载→流式问答→测试录音清理；历史/重处理/跨录音播放/删除语义 WPF 回归。
- [ ] 完整桌面首次启动、真实录音到字幕/归档/导出的操作验收。

- 2026-09-14：从 origin/main（19b229e）创建 codex/windows-native-desktop；工作区原本干净。建立计划，开始功能基线和原生技术验证。
- 2026-09-15：原生实时字幕核心与独立字幕窗口接入，14 组核心/WPF 回归通过。既有授权账号和合成语音的真实 C# WebSocket 验证收到 7 段原文；英→中翻译收到原文/译文各 1 段。配置持久化、全文归档、设备到字幕的一体化实机验收仍未完成。
- 2026-09-15：接入原生问答历史/建议/SSE/引用、云端音频下载及原生播放/倍速/seek。12 组核心测试及 WPF 问答隔离、WAV 实际打开跳转测试通过；真实云服务、其他编码及跨录音播放仍未验收。
- 2026-09-15：完成两框架可重复发布/资源探针，选择 WPF。实现原生录音核心、桌面录音面板和本地库；8 项核心测试、1 项真实录音补充、原生窗口冒烟、23 项 CLI 回归通过。x64/ARM64 可发布。完整窗口行为、云功能及最终交付未完成，阶段复合验收项保持未勾选。
- 2026-09-15：新增原生 CloudClient、DeviceLogin 和 DPAPI AccountStore，9 组核心测试通过，新增云契约和真实本机凭据加密验证。账号 UI、真实后端流程及处理队列仍待接入。
- 2026-09-15：接入账号窗口、后台上传恢复及云端录音库分页/转写/摘要阅读。10 组核心测试和原生窗口回归通过；增加退出账号后丢弃延迟响应的测试。网络验证仍使用测试响应；真实云端、字幕、问答和完整回顾操作尚未完成，阶段验收不勾选。
