# 原生云端完整窗口验证（2026-09-16）

## 回顾与版本补验

2026-09-16 使用完整 x64 自包含应用 `cb77548ab3d44e45bdc2b167a5c73dc7`，报告源码 `80448a4` 加随后提交为 `d0ac43d` 的推荐独立加载改动。独立样本目录 `build/native-desktop-validation/cloud-review-ui-01`；复用既有授权账号、9.417 秒合成语音，没有采集系统或麦克风，没有修改用户已有云录音。

实际键鼠操作、真实服务及视频复核 **5 项通过、2 项证据不足**：

- 上传并转写完成，自动关联本机/云副本；默认窗口读取三个真实转写片段。
- 从“暂无摘要”发起摘要生成，结果包含正文、要点、话题、决策、行动项和时间线。默认窗口可见前几段；未全验长摘要滚动。
- 问答返回完整回答和三个可见引用。本轮未点击引用，不重复记为音频定位验收。
- 原生保存对话框导出 TXT 成功，1085 B，实查摘要/时间线/逐字稿均存在，SHA256 `f82627e9aef983abf02c3b4c5874aa684003172ba17cd90f4134ea70ff675f7e`。
- 选择首次转写版本后重新转写，列表保留 11:42 与 11:48 两个成功版本，选择新版本后读取其三个片段。未覆盖失败分块、网络失败或大量版本。
- 确认框大部分在固定录屏区域外，保存对话框右侧略裁剪；完整弹窗布局仍为证据不足。
- 问答在输入下一条之前已经完成，真实服务下的草稿时序未覆盖，仍为证据不足；此前受控草稿回归与视频独立保留。

本地 `acceptance.html` / `acceptance.json` 关联两段无声 10 fps 视频：`cloud-flow.mp4` 285.8 秒、2858 帧、34 关键帧；`version-flow.mp4` 140.9 秒、1409 帧、14 关键帧。已查看联系表及摘要/问答/导出/版本关键帧原图，文件哈希与时间引用门禁通过；不排除帧间短闪。视频含账号和列表信息，只留本地，不提交仓库。

应用通过退出入口正常结束；现有受约束 fixture 清理入口仅删除本轮关联云录音、隔离缓存与 DPAPI 账号副本，保留本机 415,378 B 音频及导出文件。`cleanup.json` 三项清理/保留标记通过，临时凭据文件不存在，测试进程已退出。此次仅补验与文档，没有修改生产代码，也未把默认窗口单一样本提升为阶段整体验收完成。

## 早期上传与引用验证

使用既有授权账号、独立验证目录和 9.418 秒合成语音，在完整 x64 原生应用实际点击上传与转写。未操作用户已有录音内容。

## 结果与修复

- 首次真实上传、转写完成，但需手动刷新才出现云端副本入口。现在处理完成通知按 ID 拉取该录音、合并列表并保留本机选择；退出账号、旧凭据、已删除关联的延迟响应不回填。
- 首次默认窗口中逐字稿被版本区和播放器挤出视口。现在版本操作独立标签，音频播放区可折叠，详情可滚动，标题单行省略；保留有限高度以维持列表虚拟化。修复后默认窗口实际显示三段转写，底部操作需滚动。
- 第二次真实上传无需手动刷新即出现云端内容入口。最大化窗口实际问答返回“The team will review the design tomorrow.”，引用第二段；双击引用后加载云音频，定位到 00:00:03 并选中对应段落。
- 发现测试目录未隔离云内容缓存与说话人存储；改为 App 注入相同应用目录。最终发布版本重启读取测试录音成功，缓存实际写入 fixture/CloudContent。

## 本地证据

视频保留本地，含账号显示信息，不提交仓库。

| 路径（仓库 build/native-desktop-validation 下） | 内容 |
| --- | --- |
| cloud-ui-55def42-01/acceptance.html | 修复前两段视频：默认布局失败 1 项；真实问答及引用定位通过 2 项 |
| cloud-ui-fixed-01/acceptance.html | 修复后视频：自动云端入口、默认转写可读通过 2 项；2396 原始帧、6 关键帧 |
| core-tests-15bf6c2438e34f04ba5b6b39793aae25 | 最终核心 32 组回归通过 |
| cloud-ui-fixed-01/cleanup.json | 专属云样本、临时凭据清理记录；保留本机音频 |

完整 WPF 回归通过，包括新增 1120×760／800×540 转写/问答视口、滚动可达性、完成自动关联、退出后的延迟响应。最终双架构发布报告：`build/native-desktop-release/c4424c3a37db4c6ebc99d37d5b887116/release-report.json`，基于 f34e420 加本轮未提交改动，self-contained；x64 ZIP 70,967,127 B，ARM64 ZIP 65,365,125 B。ARM64 仅构建，未实机运行。

视频分析修复使用帧时间戳及 passthrough 解码，避免平均帧率补帧误差。10 fps、无声音，无法排除帧间短闪；首次窗口最大化后的裁剪画面不用于全窗口通过判定。最终缓存注入版本重启只有实际 UI 与落盘检查，未新增视频。摘要、重处理、异常网络、登录对话框、全部尺寸/DPI、跨录音完整操作仍未验收，不勾选阶段整体完成。
